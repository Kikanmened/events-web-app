# Origin of the local Events API, without a trailing slash.
# Leave empty to use the Vite proxy (http://localhost:3000/api/...).
# If your API runs on another port, set it here or change the proxy in vite.config.js.
VITE_API_URL=
